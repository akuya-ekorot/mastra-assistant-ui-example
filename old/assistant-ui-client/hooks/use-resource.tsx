/**
 * Hook to get the current resource.
 * Treat this as something similar to a hook that returns a user, or organization
 *
 * @returns The current resource
 */
export const useResource = () => {
	return "default-resource";
};
